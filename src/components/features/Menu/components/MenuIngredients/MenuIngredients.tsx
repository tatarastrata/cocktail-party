import React from 'react';
import styles from './MenuIngredients.module.scss';
import { IMenuIngredientsPropTypes } from './MenuIngredientsPropTypes';

const MenuIngredients: React.FC<IMenuIngredientsPropTypes> = ({}) => {
  return <div className={styles.MenuIngredients}>MenuIngredients</div>;
};

export default MenuIngredients;
